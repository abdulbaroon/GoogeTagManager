import ResetPassword from '@/components/ResetPassword'
import React, { Suspense } from 'react'

const page = () => {
  return (
   <Suspense>
    <ResetPassword/>
   </Suspense>
  )
}

export default page